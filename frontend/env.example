# Next.js API Base URL
# For Docker: use http://localhost (Nginx proxies /api/ to backend)
# For local dev: use http://localhost:8000
# Note: endpoints already include /api/ prefix, so baseURL should NOT include /api
NEXT_PUBLIC_API_BASE_URL=http://localhost

# Next.js Output Mode (standalone for Docker)
NEXT_OUTPUT=standalone

